## Variable and strings
# 3 types of data-types : int,float,complex numbers
integer = 1
decimal = 1.0
polar = 1+1j
# print(
# f'integer : {type(integer)} \ndecimal : {type(decimal)} \npolar : {type(polar)} ')

# Strings
apple = 'Apple is a fruit'

# Add a apostropie in between
banana = "Banana is' a fruit"

# Datatype operations addition subtraction multliplication division.
# Logical oprations

# print(bool([]), bool({}), bool(()), bool(0))

# The expression x and y first evaluates x; if x is false, its value is returned;
# otherwise, y is evaluated and the resulting value is returned.
# print((100 and 2 and 5))

#print(100000000000000000 or 0)
#print(not 2)
# Forany variables the syntax is name.action()
first = 'I am'
last = 'the power'
total = first + ' '+last
k = ' ' + first.lstrip() + ' '
print(k)
